&НаКлиенте
Процедура ТестПоиска(Команда)
    ТестПоискаНаСервере();
КонецПроцедуры

&НаСервере
Процедура ТестПоискаНаСервере()
    Попытка
        Поиск = New("AddIn.NativeApiSearch");
    Исключение
        Сообщить("Ошибка загрузки компоненты. Проверьте регистрацию.");
        Возврат;
    КонецПопытки;
    
    // Тестовые данные
    Данные = "[
        {""id"":""1"",""text"":""Иван Петров работает в компании""},
        {""id"":""2"",""text"":""Петр Иванов - директор""},
        {""id"":""3"",""text"":""Компания занимается разработкой""},
        {""id"":""4"",""text"":""Сотрудники компании работают над проектом""},
        {""id"":""5"",""text"":""Разработка нового продукта""}
    ]";
    
    Поиск.КоллекцияJSON = Данные;
    Количество = Поиск.ПостроитьИндекс();
    Сообщить("Индекс построен: " + Количество);
    
    Результаты = Поиск.Поиск("компания", 10);
    Если Результаты <> "" Тогда
        Чтение = JsonReader(Результаты);
        Массив = Чтение.Read();
        Для Каждого Элемент Из Массив Цикл
            Сообщить("ID: " + Элемент.id + ", Текст: " + Элемент.text + ", Оценка: " + Элемент.score);
        КонецЦикла;
    КонецЕсли;
КонецПроцедуры