Процедура ТестПоиска()

    // Создаём объект компоненты
    ОбъектВК = Новый("AddIn.NativeApiSearch.NativeApiSearch");

    // Тестовые данные — 1С передаёт JSON-массив
    ОбъектВК.КоллекцияJSON =
        "[
          {""id"":""1"",""text"":""Кофе зерновой"",""extra"":""AR-001""},
          {""id"":""2"",""text"":""Чай чёрный"",""extra"":""AR-002""},
          {""id"":""3"",""text"":""Кофе растворимый"",""extra"":""AR-003""}
        ]";

    // Строим индекс
    КолВо = ОбъектВК.ПостроитьИндекс();
    Сообщить("Проиндексировано: " + КолВо);   // должно быть 3

    // Ищем "кофе"
    ТекстJSON = ОбъектВК.Поиск("кофе", 10);
    Сообщить("Результат JSON: " + ТекстJSON);

    // Разбираем
    Чтение = Новый ЧтениеJSON;
    Чтение.УстановитьСтроку(ТекстJSON);
    Результаты = ПрочитатьJSON(Чтение, Истина);

    Для Каждого Найденный Из Результаты Цикл
        Сообщить("Найдено: " + Найденный.Ид + " | " + Найденный.Текст);
    КонецЦикла;

КонецПроцедуры
